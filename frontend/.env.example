# Base URL of the deployed backend API (must include the /api suffix).
# Required in production - without this, the app silently falls back to
# http://localhost:8000/api, which will not work for real visitors.
VITE_API_URL=http://localhost:8000/api
